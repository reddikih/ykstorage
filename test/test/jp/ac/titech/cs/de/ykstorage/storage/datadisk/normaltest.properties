buffer.size=64MB

cachedisk.size=64MB

buffer.threshold=1.0

spindown.threshold.time=2147483648

ykstorage.home=.

data.dir=/data

disk.file.prefix=/sd

device.file.prefix=/dev/sd

buffer.number=1

cachedisk.number=0

datadisk.number=2

diskgroup.members=2

replica.level=1

storage.manager.factory=NormalStorageManagerFactory

block.size=32B

server.port=9999

device.characters=b,c,d

