buffer.size=64MB

cachedisk.size=64MB

buffer.threshold=1.0

spindown.threshold.time=10.0

ykstorage.home=.

data.dir=/data

disk.file.prefix=/sd

device.file.prefix=/dev/sd

cachedisk.number=0

datadisk.number=2

diskgroup.members=2

replica.level=1

storage.manager.factory=MAIDStorageManagerFactory

block.size=32B

server.port=9999

device.characters=b,c,d

